{
  // Specify "require" for CommonJS
  "require": "ts-node/register",
  "extensions": ["ts", "tsx"],
  "spec": ["test/**/*.ts", "test/**/*.bs.js"],
  "reporter": "mocha-reporter",
  "timeout": 30000, //Mostly integration tests so just precaution to stop ci from breaking
  "exit": true
}
