type Pair @entity(immutable: true) {
  id: Bytes!
  token0: Bytes!
  token1: Bytes!
  name: String!
}

type Swap @entity(immutable: false) {
  id: ID!
  pair: Bytes!
  sender: Bytes!
  amount: BigInt!
}

type Tick @entity(immutable: false) {
  id: ID!
  height: BigInt!
}
