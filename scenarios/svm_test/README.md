# svm_test

Minimal SVM scenario covering the `indexer.onSlot` API and the SVM-specific
filter decoder in `packages/envio/src/Main.res`.

## Run

```bash
pnpm install
pnpm exec envio codegen
pnpm test
```
