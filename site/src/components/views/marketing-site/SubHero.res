@react.component
let make = () => {
  <p>
    {"Index data from custom smart contracts and query it in your app in real time. Unlock your potential with infrastructure that gives incredible performance and takes minimal maintenance. 
"->React.string}
  </p>
}
