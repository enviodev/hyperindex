# yaml-language-server: $schema=../../../envio/svm.schema.json
# Same shape as svm-metaplex-config.yaml but with hypersync_config omitted, so
# the endpoint must resolve to the default Solana mainnet HyperSync URL.
name: Solana indexer
ecosystem: svm
chains:
  - start_block: 422000000
    experimental:
      programs:
        - name: TokenMetadata
          program_id: metaqbxxUerdq28cj1RbAWkYQm3ybzjb6a8bt518x1s
          instructions:
            - name: CreateMetadataAccountV3
              discriminator: "0x21"
