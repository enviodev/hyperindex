---
source: packages/cli/src/executor/init.rs
expression: "next_steps_message(Path::new(\"my-$HOME-`tmp`\"), PackageManager::Npm)"
---

Your indexer is ready! Pick how you'd like to run it:

  1. cd 'my-$HOME-`tmp`' && npm test    # run the tests (recommended for AI)
  2. cd 'my-$HOME-`tmp`' && npm dev     # run locally
  3. cd 'my-$HOME-`tmp`' && npm start   # run in production
