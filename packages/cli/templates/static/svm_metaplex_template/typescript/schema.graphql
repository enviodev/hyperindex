type TokenMetadataAccount {
  id: ID!
  mint: String!
  updateAuthority: String
  name: String
  symbol: String
  uri: String
  lastUpdatedSlot: Int!
  updateCount: Int!
  createdAtSlot: Int!
  lastTxSignature: String
}

type ProgramStats {
  id: ID!
  totalInstructions: Int!
  createCount: Int!
  updateCount: Int!
}
