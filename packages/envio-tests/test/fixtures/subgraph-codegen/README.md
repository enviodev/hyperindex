Golden `graph codegen` output for `SubgraphCodegenGolden_test.res`.

Regenerate and check (does not need to be a workspace package):

```
pnpm dlx @graphprotocol/graph-cli@0.97.1 codegen
bash packages/envio-tests/test/fixtures/subgraph-codegen/check.sh
```
