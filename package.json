{
  "name": "hyperindex",
  "private": true,
  "type": "module",
  "scripts": {
    "test:templates": "pnpm --filter e2e-tests test:templates",
    "test:e2e": "pnpm --filter e2e-tests test:e2e",
    "test": "for d in e2e_test test_codegen fuel_test svm_test; do echo \"=== scenarios/$d ===\" && (cd \"scenarios/$d\" && if [ \"$d\" = test_codegen ]; then ln -sf ../../.pnpmfile.cjs .pnpmfile.cjs; fi && pnpm exec envio codegen && pnpm test) || exit 1; done"
  },
  "devDependencies": {},
  "pnpm": {
    "overrides": {
      "react-dom": "19.2.3"
    }
  }
}
