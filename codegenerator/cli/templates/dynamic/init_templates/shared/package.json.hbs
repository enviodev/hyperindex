{
  "name": "{{project_name}}",
  "version": "0.1.0",
  "scripts": {
    {{#if is_typescript}}
      "clean": "tsc --clean",
      "build": "tsc --build",
      "watch": "tsc --watch",
    {{/if}}
    {{#if is_rescript}}
      "clean": "rescript clean -with-deps",
      "build": "rescript build -with-deps",
      "watch": "rescript build -w -with-deps",
    {{/if}}
      "codegen": "envio codegen",
      "start": "{{#if is_rescript}}pnpm build &&{{/if}}{{#if is_typescript}}ts-{{/if}}node generated/src/Index.bs.js"
  },
  "devDependencies": {
    {{#if is_rescript}}
    "@glennsl/rescript-fetch": "^0.2.0",
    "@greenlabs/ppx-spice": "0.1.14",
    "@ryyppy/rescript-promise": "^2.1.0",
    "express": "^4.17.1",
    "rescript-envsafe": "^1.3.0",
    "rescript-express": "^0.4.1",
    "rescript-struct": "^4.1.1",
    {{/if}}
    {{#if is_typescript}}
    "@types/node": "^18.16.1",
    "typescript": "^5.0.4",
    {{/if}}
    "rescript": "^10.1.2"
  },
  "dependencies": {
    {{#if is_typescript}}
    "ts-node": "^10.9.1",
    {{/if}}
    "envio": "{{envio_version}}"
  }
}
