{
  "setup-worktree": [
    "cd codegenerator/cli/npm/envio && pnpm install && pnpm rescript",
    "cd scenarios/test_codegen && pnpm install",
    "pnpm codegen",
    "pnpm rescript",
    "pnpm ts:build"
  ]
}
